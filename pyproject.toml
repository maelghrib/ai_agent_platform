[project]
name = "ai-agent-platform"
version = "0.1.0"
description = "A platform that enables users to create, manage, and interact with AI agents via text and voice."
readme = "README.md"
requires-python = ">=3.10.0, <4.0.0"
dependencies = []
